apiVersion: v1
kind: Service
metadata:
  name: autocorp-service
  labels:
    app: autocorp
spec:
  selector:
    app: autocorp
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8501           # public port
      targetPort: 8501   # internal Streamlit port
